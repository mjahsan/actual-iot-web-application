name: Workflow Web Application

on: push

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: Welcome
              run: echo "Below are the files of this repo"
              
            - name: List files
              run: ls
